{-
Defina uma função que permita construir polígonos regulares. 
A função recebe como parâmetros o raio e a quantidade de lados do polígono.

Não consegui fazer ainda
-}

import CodeWorld 

main :: IO()
main = drawingOf(
	coordinatePlane
	            )